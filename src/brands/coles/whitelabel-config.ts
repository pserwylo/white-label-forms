export interface IWhitelabelConfig {
    title: string
    description: string
}

export const config: IWhitelabelConfig = {
    title: "Coles Insurance",
    description: "When it comes to insurance, quality is everything.",
}