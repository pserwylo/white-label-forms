export interface IWhitelabelConfig {
    title: string
    description: string
}

export const config: IWhitelabelConfig = {
    title: "Whitelabel form PoC",
    description: "A dynamically generated form, that can be rebranded for different clients.",
}